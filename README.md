RAG API (NestJS)

This project provides a minimal NestJS-based backend that exposes a simple Retrieval-Augmented Generation (RAG) endpoint.

It uses:

NestJS for the HTTP API structure

Gemini AI (via @google/genai) for embeddings and text generation

gemini-embedding-001 for creating embeddings

FAISS for vector similarity searc